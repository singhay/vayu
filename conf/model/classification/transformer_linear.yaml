model:
  type: classification
  name: transformer_linear
  class: vayu.models.classification.transformer.TransformerLinear
  params:
    # Pretrained config name or path if not the same as model_name
    config_name: ''
    model_type: 'roberta'
    model_name_or_path: ''
    train_transformer: false

    mode: 'mean'

# Pretrained config name or path if not the same as model_name
config_name: ''
